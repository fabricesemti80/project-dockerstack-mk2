# Jellyfin - Media Server (Performance Optimized)
# https://jellyfin.org/

version: "3.9"

services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    networks:
      - proxy
    volumes:
      # Performance: Config and Cache on local LVM storage
      - type: bind
        source: /data/homelab/jellyfin/config
        target: /config
      - type: bind
        source: /data/homelab/jellyfin/cache
        target: /cache
      # Library: Shared NFS storage
      - type: bind
        source: /data/media
        target: /media
        read_only: true
    ports:
      - target: 8096
        published: 8096
        protocol: tcp
        mode: ingress
    environment:
      - JELLYFIN_PublishedServerUrl=https://jellyfin.${DOMAIN}
      - PUID=1000
      - PGID=1000
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.hostname == dkr-srv-3
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=proxy"
        - "traefik.http.routers.jellyfin.rule=Host(`jellyfin.${DOMAIN}`)"
        - "traefik.http.routers.jellyfin.entrypoints=websecure"
        - "traefik.http.routers.jellyfin.tls.certresolver=cloudflare"
        - "traefik.http.services.jellyfin.loadbalancer.server.port=8096"
        - "traefik.http.routers.jellyfin.middlewares=jellyfin-compress"
        - "traefik.http.middlewares.jellyfin-compress.compress=true"

networks:
  proxy:
    external: true
